<?xml version="1.0" encoding="utf-8"?>
<resources>
    //tạo style riêng cho button
    <style name="MyButtonStyle" parent="Widget.Material3.Button">
        <item name="backgroundTint">@null</item>
    </style>

    <style name="OtpStyle">
        <item name="android:layout_width">44dp</item>
        <item name="android:layout_height">44dp</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/bg_otp_box</item>
        <item name="android:inputType">number</item>
        <item name="android:maxLength">1</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="KeyStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">56dp</item>
        <item name="android:layout_margin">6dp</item>
        <item name="android:background">@drawable/key_background</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>
    <style name="MyFlatButton" parent="Widget.AppCompat.Button.Borderless">
        <!-- Kế thừa từ Borderless để có ít style mặc định nhất -->

        <!-- Vô hiệu hóa Tinting cho Button trên các phiên bản cũ -->
        <item name="android:backgroundTint">@null</item>
        <item name="backgroundTint">@null</item>

        <!-- Đặt màu chữ trắng -->
        <item name="android:textColor">@android:color/white</item>

        <!-- Đặt padding/font nếu cần -->
        <item name="android:textSize">18sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:paddingVertical">14dp</item>
    </style>
</resources>
